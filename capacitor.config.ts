import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.swastikchats.app',
  appName: 'Swastik Chats',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
